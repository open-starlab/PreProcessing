import os
import pandas as pd
from tqdm import tqdm

class Basketball_space_data:
  def __init__(self,data_provider,space_data_path,...)
    self.data_provider = data_provider
    ...
    
  def load_data(self):
    ...
